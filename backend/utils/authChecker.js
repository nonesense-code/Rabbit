const express = require("express");
const userModel = requrie("../models/user");
const productModel = requrie("../models/product");
const jwt = require("jsonwebtoken");

module.exports.isUserAuthorized = async (req, res) => {};
module.exports.isValidAdmin = async (req, res) => {};
